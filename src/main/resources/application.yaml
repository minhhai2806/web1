server:
  port: 8080
  servlet:
    context-path: /identity1
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identityservice"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
